eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  info:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: info 

info:
  author: Rainvare
  mail: indirarequiz@gmail.com
server:
  port: ${PORT:8091}
message: Configuraciones genéricas

spring:
  cloud:
    ineutils:
      timeout-seconds: 10
logging:
  level:
    org.springframework.cloud.commons.util.InetUtils: trace
